# Copie este arquivo para config.ini e preencha os valores.
# config.ini está no .gitignore — nunca será enviado ao repositório.

[intranet]
usuario = joao.mendes
senha   = SUA_SENHA_AQUI

[relatorio]
linha_produto = 5

[github]
token  = ghp_SEU_TOKEN_AQUI
owner  = joaovitorsmg-cmd
repo   = auditoria
path   = analise-compra-agricola/data/dados.json
branch = main
